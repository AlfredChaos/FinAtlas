# PRD：FinAtlas（个人财务管理平台｜文件导入 + AI Agent）

产品名称：FinAtlas  
版本：v0.3（引入 Agno 作为 AI 基座；架构：自建 PostgreSQL/Redis/RabbitMQ，保留 R2）  
作者：SOLO（产品经理视角）  
日期：2026-05-22  
目标用户：单用户自用（后续可扩展多用户，但不作为当前版本范围）  

---

## 1. 背景与问题陈述

用户现状：
- 长期定投基金平台：支付宝、天天基金、且慢
- 日常消费：招商银行储蓄卡（借记卡流水）、中信银行信用卡（账单）
- 收入：每月工资收入

主要痛点：
1) 资产分散在多个平台，缺少统一视图：净资产、现金流、投资盈亏难以总览  
2) 消费记录杂乱：分类不稳定、难以形成预算与改善策略  
3) 投资缺少方法论：不知道风险暴露、资产配置是否合理、定投与再平衡如何做  
4) 数据接入现实约束：更倾向于通过上传 PDF/Excel/CSV/截图，而非实时 API 打通  

因此需要一个“个人财务管理平台 + AI Agent”，通过**文件导入**实现数据汇总，并在结构化数据基础上提供可解释的分析与建议。

---

## 2. 产品目标（Goals）

### 2.1 核心目标
G1. 统一总览：用一个平台看到个人财务全局（资产、负债、现金流、消费结构、投资表现）  
G2. 低成本接入：通过上传文件（CSV/Excel/PDF/图片）完成数据导入，并支持每月一次更新  
G3. 可解释的 AI 辅助：AI Agent 用于“分类纠错、报告生成、问答检索、投资体检建议”，所有结论可追溯到原始数据/账单证据  
G4. 可靠可恢复：导入任务可重试、可回放、可去重，解析版本升级后可重跑生成结构化结果  

### 2.2 成功指标（Success Metrics）
（单用户版本以可用性与准确性为主，后续可扩展为运营指标）
- M1. 数据覆盖率：当月关键来源覆盖（招行/中信/至少一个基金平台）≥ 90%（以金额或记录数衡量）  
- M2. 导入成功率：CSV/Excel 导入成功 ≥ 99%；PDF 导入成功 ≥ 90%；图片导入成功 ≥ 80%  
- M3. 分类可用性：自动分类准确率（用户确认后）≥ 85%；未分类占比 ≤ 5%  
- M4. 月报生成：每次月度导入后可在 2 分钟内生成月报快照（不包含重型 OCR 的等待时间）  

---

## 3. 非目标（Non-Goals）

当前阶段明确不做：
- NG1. 实时自动同步（支付宝/银行/基金平台实时 API 全自动拉取）  
- NG2. 交易执行/自动下单/代客理财  
- NG3. 多用户体系、计费、复杂权限（当前仅自用）  
- NG4. 深度量化/高频策略（以个人理财体检与框架建议为主）  

---

## 4. 用户画像与核心场景

### 4.1 用户画像
- 单用户：有稳定工资收入；多平台基金定投；消费渠道多；希望获得“看得懂、能执行”的改善与理财建议  

### 4.2 核心使用场景（按优先级）
S1（月度）导入当月：上传招行流水 + 中信信用卡账单 + 基金交易记录（支付宝/天天基金/且慢若干）  
S2（月度）生成月报：本月收入/支出/结余、消费结构、预算偏差、净资产变化、投资净投入与收益  
S3（随时）问答：  
- “上个月外卖花了多少？占比多少？”  
- “这个月信用卡还款日之前需要准备多少现金？”  
- “我今年以来投资总收益是多少？主要来自哪个平台？”  
S4（持续）分类纠错：系统给出候选分类，用户一键确认；系统学习并形成规则  
S5（季度/半年）投资体检：集中度、风格暴露、波动与回撤、再平衡建议、应急金与现金管理建议  

---

## 5. 产品范围与版本规划

### 5.1 MVP（v1）范围（建议）
1) 账户与来源管理（招行/中信/支付宝基金/天天基金/且慢）  
2) 文件导入（CSV/Excel/PDF/图片）：上传、解析、去重、入库、错误回显  
3) 交易流水与投资交易统一台账  
4) 消费分类：规则引擎 + AI 辅助（可选）+ 人工确认闭环  
5) 月度快照报表：现金流、消费结构、净资产、投资概览  
6) 基础问答：在结构化数据上回答（SQL/聚合），并提供证据链  

### 5.2 v2（增强）
1) 更强的 PDF 模板识别与抽取（多版本模板、容错更强）  
2) 投资体检模块：风险评估问卷、资产配置建议、再平衡规则生成  
3) 预算管理：按类别预算、预警与建议  

### 5.3 v3（长期）
1) 多用户/家庭账本（租户隔离、共享与权限）  
2) 更丰富的数据接入（邮箱账单、更多银行与券商）  

---

## 6. 关键体验原则（产品要求）

P1. **以证据为中心**：所有统计与建议都可点击追溯到“导入文件 → 原始行/原始页”  
P2. **允许不完美导入**：PDF/图片允许低置信度；系统必须能标记不确定字段并让用户快速修正  
P3. **幂等与去重优先**：同一个文件/同一笔交易重复导入不能污染台账  
P4. **月度节奏优化**：核心链路围绕“每月一次导入 → 自动生成月报快照”  

---

## 7. 功能需求（Functional Requirements）

### 7.1 基础模块：账户/来源管理
**目标**：统一管理数据来源、导入模板、账户映射。

需求：
- FR-ACCT-01 支持创建/编辑账户（招行借记卡、中信信用卡、各基金平台账户）
- FR-ACCT-02 账户关联来源类型（bank_debit、credit_card、fund_platform）
- FR-ACCT-03 支持设置账期规则（尤其信用卡账单周期）
- FR-ACCT-04 支持为来源配置“导入模板/字段映射”（用于 CSV/Excel）

验收：
- 能为每个来源选择模板并成功导入一份样例文件  

---

### 7.2 文件导入模块（核心）
**目标**：支持 CSV/Excel/PDF/图片上传，异步解析、去重、结构化入库，并输出可回溯的导入结果。

#### 7.2.1 上传与存储
- FR-IMP-01 支持上传文件类型：`.csv .xlsx .xls .pdf .png .jpg .jpeg`
- FR-IMP-02 上传后原始文件存入对象存储（R2），并记录对象 key、hash、来源类型、账期、上传时间
- FR-IMP-03 支持同文件 hash 去重：若 hash 已存在，允许复用历史解析结果或提示“可能重复”

#### 7.2.2 导入作业（Import Job）状态机
导入作业 `import_job` 必须具备以下状态（至少）：
- `uploaded`（已上传，待处理）
- `extracting`（抽取中：PDF 文本/表格、图片 OCR、表格识别）
- `normalizing`（字段映射与标准化）
- `deduping`（生成去重键、冲突处理）
- `persisting`（批量落库）
- `needs_review`（存在低置信度/冲突，需要用户确认）
- `completed`（完成）
- `failed`（失败，可重试）

要求：
- FR-IMP-04 每个状态有开始/结束时间、错误信息、重试次数
- FR-IMP-05 支持“从某一步重新开始”（例如只重跑 OCR/抽取，不重跑已确认的分类）

#### 7.2.3 解析与抽取（多格式）
- FR-IMP-06 CSV/Excel：支持字段映射配置（列名差异、日期格式、金额正负、币种）
- FR-IMP-07 PDF：支持区分文本型 vs 扫描型，文本型优先直接抽取；扫描型走 OCR
- FR-IMP-08 图片：OCR + 结构化抽取（用于补录/少量交易）
- FR-IMP-09 抽取结果必须写入“中间产物”（R2），便于回放与调试

#### 7.2.4 去重与幂等（强制）
- FR-IMP-10 必须为每条结构化记录生成 `dedupe_key`（按来源类型定义）
- FR-IMP-11 D1 侧必须有唯一约束（`account_id + dedupe_key` 或 `import_job_id + row_hash`）兜底
- FR-IMP-12 重复记录策略：默认忽略；也允许标记为“疑似重复”进入复核列表

#### 7.2.5 导入结果回显与复核
- FR-IMP-13 导入结束后展示：成功条数、跳过重复条数、失败条数、待复核条数
- FR-IMP-14 待复核项需展示“证据链”：来源文件 + 页码/行号 + 原始文本片段（若可用）
- FR-IMP-15 用户可在 UI 中修正：日期、金额、对手方、分类、交易类型、备注等

验收（MVP）：
- 导入一份招行 CSV 能生成 N 条交易并可在明细页查看  
- 导入同一文件两次不产生重复交易  
- PDF 导入失败时能给出可定位的错误提示与重试入口  

---

### 7.3 台账与数据展示
**目标**：提供统一的交易明细与投资明细，并可按时间/账户/分类/平台筛选。

需求：
- FR-LED-01 交易明细列表：支持筛选（月份、账户、分类、关键词、金额范围）
- FR-LED-02 明细详情页：展示结构化字段 + 原始证据（文件链接/页码/行号）
- FR-LED-03 投资交易列表：按平台/基金/类型（申购/赎回/分红/手续费）过滤
- FR-LED-04 支持手动新增/编辑一条记录（用于补漏）

验收：
- 任意一条交易可追溯到其导入来源  

---

### 7.4 消费分类与规则学习
**目标**：把“杂乱消费”变成可分析的结构化分类，并持续变准。

需求：
- FR-CAT-01 默认分类体系（可自定义扩展/合并/停用）
- FR-CAT-02 自动分类规则（优先级从高到低）：
  1) 用户显式规则（商户关键词/对手方/卡号后四位/备注正则）
  2) 历史确认映射（同商户/同摘要 → 同分类）
  3) AI 候选分类（可选开关），给出 Top-K + 置信度 + 依据
- FR-CAT-03 分类确认：用户一键确认/批量确认；确认后写入“学习样本”
- FR-CAT-04 支持“分类回滚/重新训练规则”（至少支持撤销最近一次批量操作）

验收：
- 用户确认 20 条外卖后，后续外卖类交易自动命中分类  

---

### 7.5 月度报表（快照）
**目标**：每月导入后生成固定结构的月报，并保存为快照，后续访问无需重算。

报表结构（建议固定）：
- ① 本月概览：收入、支出、结余、储蓄率
- ② 消费结构：Top 类别、Top 商户、异常支出
- ③ 现金流与负债：信用卡已出账/未出账/还款计划（MVP 可先简化）
- ④ 投资概览：净投入、赎回、分红、手续费（收益拆分 v2 做）
- ⑤ 建议与行动项：预算建议、异常提醒、投资体检入口

需求：
- FR-RPT-01 生成条件：某账期导入完成（或用户手动触发）
- FR-RPT-02 报表保存为 `monthly_summary`（结构化）+ 可选 `markdown/html`（可阅读）
- FR-RPT-03 报表中每个关键数字可点击查看明细构成（证据链）

验收：
- 生成 2026-05 月报，能从“外卖支出”点击进入对应交易列表  

---

### 7.6 AI Agent 能力（MVP 约束为“可解释”）
**定位**：Agent 不是替你做投资决策，而是帮助你“看清现状 → 找出问题 → 提供框架化建议与可执行动作”。

**实现基座（约束）**：
- 本项目 AI Agent 运行时采用开源项目 **Agno** 作为基座（Agent/Team/Workflow + AgentOS 运行时 + 可观测与治理能力）：https://github.com/agno-agi/agno
- 产品侧要求 Agno 仅作为“Agent 编排与运行时”，核心业务数据与权限仍由 FinAtlas 后端负责；Agent 的所有关键输出必须可回溯到结构化数据与证据链（避免“黑盒建议”）。

#### 7.6.1 问答（Query Agent）
- FR-AI-01 支持自然语言问答（基于结构化数据查询）
- FR-AI-02 输出必须包含：结论 + 计算口径 + 可追溯链接（指向明细筛选）
- FR-AI-03 若数据不足，应明确提示缺口（例如缺少某平台当月数据）

示例问题：
- “我上个月在餐饮花了多少？与上上个月相比变化？”  
- “这个月中信信用卡已出账金额是多少？”  
- “今年以来基金净投入是多少？”  

#### 7.6.2 分类助手（Categorization Agent）
- FR-AI-04 对未分类/低置信度项输出候选分类与理由
- FR-AI-05 用户确认后写入规则/样本，提升后续命中

#### 7.6.3 投资体检（Investment Checkup，v2 为主，MVP 可做入口+基础规则）
输出内容应以框架化为主：
- 风险偏好与资产配置建议（需问卷或参数化设置）
- 集中度提示（平台/单基金/单类型占比）
- 费用与摩擦成本提示（申赎费率、频繁操作）
- 再平衡建议（规则化、可解释、可执行）

---

## 8. 数据需求与数据模型（概要）

> 以“证据链 + 幂等去重 + 月度快照”为核心。

### 8.1 核心实体（建议）
- `account`：账户/平台（银行账户、信用卡、基金平台）
- `import_job`：导入作业
- `import_item`：导入条目（原始行/抽取行）
- `transaction`：现金流交易（收入/支出/转账/还款等）
- `investment_trade`：投资交易（申购/赎回/分红/手续费）
- `category`：分类
- `category_rule`：规则（关键词/正则/映射）
- `monthly_summary`：月报快照

### 8.2 关键字段要求（验收口径）
每条 `transaction` 最少应具备：
- `occurred_at`（交易发生/入账日期，需区分）
- `amount`（统一币种、正负规则一致）
- `account_id`
- `counterparty`（对手方/商户）
- `raw_description`（原摘要）
- `category_id`（可为空但需进入复核）
- `source_import_job_id + source_pointer`（证据定位：行号/页码/块）
- `dedupe_key`

每条 `investment_trade` 最少应具备：
- `platform`（支付宝/天天基金/且慢）
- `fund_code/name`
- `trade_type`（buy/sell/dividend/fee/convert）
- `trade_date`（申请日/确认日需明确）
- `amount` 与/或 `shares`
- `dedupe_key` + `source_pointer`

---

## 9. 技术与集成约束（面向产品的要求表达）

### 9.1 部署与组件（既定方向）
- 自建服务器承载 Web/API/解析任务/AI 调用
- 数据与文件：
  - **Cloudflare R2**：原始文件 + 中间产物（抽取 JSON/OCR 结果等）
  - **自建 PostgreSQL**：结构化数据与快照（交易台账、分类、导入作业、月报快照等）
- 异步与缓存：
  - **自建 RabbitMQ**：消息队列（导入任务、解析步骤、重试与延迟）
  - **自建 Redis**：锁、速率限制、幂等缓存、任务协调（例如避免同一 job 并发处理）
- AI 基座：
  - **Agno（AgentOS 运行时）**：承载 Agent 的会话/记忆/知识/工具调用编排、可观测与治理能力；与 FinAtlas 后端通过内部 API 交互（查询台账、生成报表、触发复核流等）

### 9.2 产品侧约束（必须满足）
- TC-01 导入任务必须异步化，可重试、可回放
- TC-02 所有统计与 AI 输出必须可追溯到明细与证据
- TC-03 不允许“静默吞错”：解析失败必须可定位（文件、页码/行号、步骤、错误原因）
- TC-04 数据安全：R2 私有桶；下载通过短期签名 URL；最小权限 API Token 管理
- TC-05 AI 治理：对“会影响资金安全/隐私”的能力必须具备人审或确认机制（例如批量分类、批量改动、导出/删除数据、生成投资调整建议时的确认）
- TC-06 AI 可观测：每次 Agent 运行需保留 trace / 运行记录（至少包含输入、输出摘要、引用证据、所调用工具与结果），用于回溯与纠错

---

## 10. 权限与安全（单用户版）

需求：
- SEC-01 登录鉴权（单用户可用简单方案，但需支持后续扩展）
- SEC-02 R2 访问必须走签名 URL 或后端代理，不提供公共读
- SEC-03 PostgreSQL/Redis/RabbitMQ/R2 凭证只保存在服务端（环境变量/密钥管理）
- SEC-04 审计日志：导入、删除、批量分类、手动编辑等操作记录

---

## 11. 运营与可观测性（单用户也需要）

需求：
- OBS-01 导入作业可观测：每步耗时、失败率、重试次数
- OBS-02 队列积压监控：backlog、处理速率（用于调整并发、预取数量、重试/死信策略）
- OBS-03 关键统计口径版本化：当算法或规则变更，月报快照要记录版本号

---

## 12. 交互与页面清单（建议）

1) 首页 Dashboard：净资产概览、当月现金流、导入状态、快捷问答入口  
2) 导入中心：
   - 上传入口（选择来源 + 账期）
   - 导入作业列表（状态、耗时、结果）
   - 作业详情（步骤、错误、复核项）
3) 交易明细页（筛选 + 详情证据链）
4) 分类管理页（分类树 + 规则列表 + 命中统计）
5) 月报页（按月份浏览 + 可下钻）
6) AI 助手页（问答 + 最近问题 + 引用明细）

---

## 13. 验收标准（Acceptance Criteria）

MVP 验收：
1) 能导入至少两类来源：
   - 招行流水（CSV/Excel）
   - 中信信用卡账单（CSV/Excel 或 PDF）
2) 重复导入同一文件不产生重复交易（幂等成立）
3) 每条交易可追溯到来源文件（至少到文件 + 行号；PDF 至少到文件 + 页码/块）
4) 月报可生成并可下钻到明细列表
5) 分类纠错闭环可用：用户确认后提升后续命中
6) 队列与任务可重试：解析失败不会导致系统不可用

---

## 14. 风险与对策

R1. PDF/图片抽取误差导致数据不准  
- 对策：证据链 + 低置信度复核 + 中间产物存档 + 解析版本化  

R2. 自建数据库/队列带来运维成本与可靠性风险（备份、升级、监控、容灾）  
- 对策：标准化运维（自动备份、监控告警、容量规划）；RabbitMQ 采用死信队列（DLQ）与可观测指标；数据库采用迁移脚本与回滚策略；R2 仍作为原始数据“真相源”便于恢复  

R3. 投资建议合规与用户预期  
- 对策：以“框架建议/风险提示/规则化再平衡”为主，避免强推荐具体标的；所有建议给出依据与限制条件  

R4. 凭证泄露风险（R2/PostgreSQL/Redis/RabbitMQ/模型密钥）  
- 对策：最小权限 Token、定期轮换、服务端密钥管理、审计日志  

R5. AI 基座升级/行为变化带来的回归风险（Agno 版本升级、模型提供方变化）  
- 对策：锁定版本与变更评审；关键链路（问答口径、月报生成、分类建议）增加回归用例；Agent 输出必须引用证据并可复算（降低模型漂移带来的“错误结论”影响）

---

## 15. 已确认业务口径（Resolved Decisions）

1) 信用卡账单口径：现金流默认以“还款日”记账，用于现金流与结余曲线。  
2) 基金数据口径：默认以“申请日”记账，同时保留“确认日”作为可选字段或视图；分红默认按“再投资”处理，并支持可选“现金分红”。  
3) 分类体系：采用“通用预算分类”作为默认基线，并支持自定义扩展、合并、停用。  
4) AI 形态：优先提供“聊天问答”体验；“每月自动生成行动清单”作为后续增强能力。  

---

## 16. 附录：消息与任务（产品级约束表达）

建议将导入拆为可重入步骤（每步完成后推进下一步）：
- Step A：extract（抽取/OCR → 产出 derived JSON 到 R2）
- Step B：normalize（字段映射、金额正负、日期解析）
- Step C：dedupe（生成去重键、冲突检测）
- Step D：persist（批量写入 D1）
- Step E：review（生成待复核列表）
- Step F：summarize（生成月报快照）

每条队列消息建议包含：
- `jobId`
- `step`
- `attempt`
- `traceId`
- 可选：`sourceType`、`period`
